{
    "name": "wwppc",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install": "cd wwppc-server && npm install",
        "build": "cd wwppc-server && npm run build",
        "compile": "cd wwppc-server && npm run compile",
        "start": "node wwppc-server/build/server.js",
        "compilerun": "cd wwppc-server && npm run compilerun",
        "deploy": "npm run build && firebase deploy --only hosting"
    },
    "homepage": "https://github.com/WWPPC/WWPPC"
}
